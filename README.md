# root-plotline-domain

Single-page Bun + React site showing an interactive node graph for:
- `plotline.nz`
- `app.plotline.nz`
- `sites.plotline.nz`

## Run

```bash
bun install
bun dev
```

## Build

```bash
bun run build
```
